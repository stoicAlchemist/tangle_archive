<%= form_with(model: project_type, local: true) do |form| %>
  <% if project_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(project_type.errors.count, "error") %> prohibited this project_type from being saved:</h2>

      <ul>
      <% project_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :project_type_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :project_type_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
